{
  "name": "ht1-1.2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --exec babel-node --presets=@babel/preset-env -- index.js",
    "start-lbl": "nodemon --exec babel-node --presets=@babel/preset-env -- index.js --lbl",
    "start-all": "nodemon --exec babel-node --presets=@babel/preset-env -- index.js --all"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csvtojson": "^2.0.10"
  },
  "devDependencies": {
    "@babel/core": "^7.13.8",
    "@babel/node": "^7.13.0",
    "@babel/preset-env": "^7.13.9",
    "nodemon": "^2.0.7"
  },
  "nodemonConfig": {
    "ignore": [
      "*.json"
    ]
  }
}
